//1 step extract the colors
import 'package:flutter/material.dart';

class AppColors{
  //static: to do not git a object in each time we need to ude a color
  //we make object only if we want to make an opereation
  static const Color backgroundColor= Color(0xff0a0c21);
  static const Color cardColor= Color(0xff1d1e34);
  static const Color primaryColor= Color(0xffeb1455);
  static const Color grayColor= Color(0xff4c505d);
  static const Color whiteColor= Color(0xffffffff);
  //we put the frequntly used colors
  
}